services:
  - type: web
    name: nimops-company-portal
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: CANONICAL_HOSTS
        value: portal.gideonler.com
